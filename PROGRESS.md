# Progress Log

## Completed Tasks

- [x] Configure project-wide formatting standards using Prettier.
- [x] Add `format` and `format:check` scripts to `package.json`.
- [x] Create `.vscode/settings.json` to enforce Prettier and enable format on save in the workspace.
- [x] Document decision as `ADR-004` in `docs/decisions.md`.
- [x] Format all project files using `pnpm format`.
